# 需求交付流程图

```mermaid
flowchart TB
  start([开始])
  collect[需求采集]
  preReview[需求预评审]
  detailReview[需求详细评审]
  assign[需求分配]
  splitDev{按角色生成开发任务}
  devGate{所有开发任务完成?}
  splitTest{按角色生成测试任务}
  testGate{所有测试任务完成?}
  uiAccept[UI验收]
  productAccept[产品验收]
  done([产品])

  start --> collect --> preReview --> detailReview --> assign --> splitDev
  splitDev --> backend
  splitDev --> frontend
  backend --> devGate
  frontend --> devGate
  devGate -- 否 --> splitDev
  devGate -- 是 --> splitTest
  splitTest --> tester
  tester --> testGate
  testGate -- 否 --> splitTest
  testGate -- 是 --> uiAccept --> productAccept --> done

  subgraph backend[服务端开发任务]
    direction TB
    beTask1[服务端开发 A]
    beTask2[服务端开发 B]
    beTaskN[服务端开发 N]

    subgraph beFlow[服务端状态流]
      direction LR
      beSchedule[待排期] --> beTodo[待开发] --> beDoing[开发中] --> beJoint[联调中] --> beDone[开发完成]
    end

    beTask1 --> beFlow
    beTask2 --> beFlow
    beTaskN --> beFlow
  end

  subgraph frontend[前端开发任务]
    direction TB

    subgraph ios[iOS]
      direction LR
      iosSchedule[待排期] --> iosTodo[待开发] --> iosDoing[开发中] --> iosJoint[联调中] --> iosDone[开发完成]
    end

    subgraph android[安卓]
      direction LR
      androidSchedule[待排期] --> androidTodo[待开发] --> androidDoing[开发中] --> androidJoint[联调中] --> androidDone[开发完成]
    end

    subgraph miniProgram[小程序]
      direction LR
      mpSchedule[待排期] --> mpTodo[待开发] --> mpDoing[开发中] --> mpJoint[联调中] --> mpDone[开发完成]
    end

    subgraph harmony[鸿蒙]
      direction LR
      hmSchedule[待排期] --> hmTodo[待开发] --> hmDoing[开发中] --> hmJoint[联调中] --> hmDone[开发完成]
    end

    subgraph web[Web]
      direction LR
      webSchedule[待排期] --> webTodo[待开发] --> webDoing[开发中] --> webJoint[联调中] --> webDone[开发完成]
    end
  end

  subgraph tester[测试任务]
    direction LR
    testSchedule[待排期] --> testTodo[待测试] --> testDoing[测试中] --> testDone[测试完成]
  end
```

## 流程节点

| 阶段 | 控制方式 | 角色/任务 | 状态 |
| --- | --- | --- | --- |
| 需求采集 | 单节点流转 | 产品/业务 | - |
| 需求预评审 | 单节点流转 | 产品、研发、测试等评审角色 | - |
| 需求详细评审 | 单节点流转 | 产品、研发、测试等评审角色 | - |
| 需求分配 | 按角色拆分任务 | 服务端、iOS、安卓、小程序、鸿蒙、Web、测试等 | - |
| 需求开发 | 多任务并行，全部完成后汇聚 | 多个服务端开发、多个前端开发 | 待排期、待开发、开发中、联调中、开发完成 |
| 需求测试 | 多任务并行，全部完成后汇聚 | 测试角色，可按模块或测试人拆分 | 待排期、待测试、测试中、测试完成 |
| UI验收 | 单节点或多角色验收汇聚 | UI/设计 | - |
| 产品验收 | 单节点或多角色验收汇聚 | 产品 | - |
| 产品 | 终态 | - | - |

## 流转规则

1. 需求分配后，系统按参与角色生成独立任务。
2. 同一角色可以存在多个任务，例如多个服务端开发并行处理不同模块。
3. 不同角色任务并行推进，例如服务端、iOS、安卓、小程序、鸿蒙、Web 可以同步开发。
4. 需求主状态进入测试前，必须满足所有开发任务均为“开发完成”。
5. 测试、UI验收、产品验收也可以按角色或人员拆分任务，并通过“全部完成”规则控制主流程流转。
